# Dev only (overrides .env). The app calls the API same-origin at /api and Vite
# proxies it to the backend below — this avoids the API's CORS allow-list.
VITE_API_URL=/api
VITE_DEV_PROXY_TARGET=https://munlightapi-preprod.up.railway.app
