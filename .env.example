# Base URL of the munlight FastAPI backend, including the /api prefix
VITE_API_URL=http://localhost:8000/api
# Public URL for Shiprocket webhook registration (production API base)
# VITE_PUBLIC_WEBHOOK_URL=https://api.yourdomain.com/api/webhooks/shiprocket
